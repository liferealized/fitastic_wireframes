# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: postgresql
  encoding: unicode
  database: fitties_dev
  username: postgres
  password:

# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: unicode
  database: fitties_test
  username: postgres
  password:

production:
    adapter: postgresql
    encoding: unicode
    database: fitties_prod
    username: postgres
    password:
